{
  "name": "@mindmap/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Better Auth config + helpers. Google OAuth + magic-link email.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "better-auth": "^1.0.0",
    "next": "16.0.0"
  },
  "devDependencies": {
    "@mindmap/config": "workspace:*",
    "@mindmap/database": "workspace:*",
    "@mindmap/types": "workspace:*",
    "eslint": "^9.16.0",
    "eslint-plugin-import": "^2.31.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.18.0"
  }
}
