{
  "name": "@mindmap/brain",
  "version": "0.1.0",
  "private": true,
  "description": "AI: router, engines, prompts, memory. The only package that may import an AI SDK.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^0.2.16",
    "@mindmap/prompts": "workspace:*",
    "@mindmap/shared": "workspace:*",
    "@mindmap/types": "workspace:*",
    "ai": "^4.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@mindmap/config": "workspace:*",
    "typescript": "^5.6.0",
    "eslint": "^9.16.0",
    "typescript-eslint": "^8.18.0",
    "eslint-plugin-import": "^2.31.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0"
  }
}
